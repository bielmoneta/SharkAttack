import { CriticoDirective } from './critico.directive';

describe('CriticoDirective', () => {
  it('should create an instance', () => {
    const directive = new CriticoDirective();
    expect(directive).toBeTruthy();
  });
});
